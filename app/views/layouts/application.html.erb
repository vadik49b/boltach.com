<!DOCTYPE html>

<html>
  <head>
    <title><%= content_for(:title) || "Vadim Boltach" %></title>

    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta charset="UTF-8">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">

    <!-- Facebook Meta Tags -->
    <meta property="og:url" content="https://boltach.com/">
    <meta property="og:type" content="website">
    <meta property="og:title" content="Vadim Boltach">
    <meta property="og:description" content="Software Engineer at Coursera">
    <meta property="og:image" content="/avatar_logo.jpeg">

    <!-- Twitter Meta Tags -->
    <meta name="twitter:card" content="summary_large_image">
    <meta property="twitter:domain" content="boltach.com">
    <meta property="twitter:url" content="https://boltach.com/">
    <meta name="twitter:title" content="Vadim Boltach">
    <meta name="twitter:description" content="Software Engineer at Coursera">
    <meta name="twitter:image" content="/avatar_logo.jpeg">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/firacode@6.2.0/distr/fira_code.css"
    >

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%# = tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <!-- Favicon -->
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png">

    <link
      rel="icon"
      type="image/png"
      sizes="32x32"
      href="/favicon-32x32.png"
    >

    <link
      rel="icon"
      type="image/png"
      sizes="16x16"
      href="/favicon-16x16.png"
    >

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <% if authenticated? %>
      <%= button_to "Log out", session_path, method: :delete, class: "fixed top-6 right-6 px-4 py-2 text-sm text-gray-600 hover:text-gray-900 hover:bg-gray-100 rounded-lg transition-colors font-firacode cursor-pointer" %>
    <% end %>

    <%= yield %>

    <footer class="fixed bottom-0 left-0 right-0 text-left text-sm text-gray-600">
      <div class="flex flex-col gap-2 px-6 pb-6">
        <% unless authenticated? %>
          <%= link_to new_session_path, class: "relative inline-block w-6 h-6 group", aria: { label: "Sign in" } do %>
            <%= image_tag "lock.svg", class: "absolute inset-0 w-full h-full transition-opacity duration-300 group-hover:opacity-0", aria: { hidden: true } %>
            <%= image_tag "unlock.svg", class: "absolute inset-0 w-full h-full transition-opacity duration-300 opacity-0 group-hover:opacity-100", aria: { hidden: true } %>
          <% end %>
        <% end %>

        <p>© <%= Time.current.year %> Vadim Boltach</p>
      </div>
    </footer>
  </body>
</html>
